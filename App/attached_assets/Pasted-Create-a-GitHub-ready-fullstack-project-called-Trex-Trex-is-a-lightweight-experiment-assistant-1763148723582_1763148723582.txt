Create a GitHub-ready fullstack project called **Trex**.

Trex is a lightweight experiment assistant that helps ML researchers run and manage multiple ML training jobs through a conversational interface. The frontend should be fully implemented, while the backend should only contain empty files with TODO placeholders so backend teammates can fill them in later.

=====================================================
PROJECT STRUCTURE (GENERATE EXACTLY THIS)
=====================================================

trex/
    frontend/
        app/
            page.tsx
            globals.css
        components/
            ChatWindow.tsx
            ChatInput.tsx
            ChatBubble.tsx
            RunsGrid.tsx
            RunCard.tsx
            PlotViewer.tsx
        lib/
            api.ts
            llm.ts
            types.ts
        public/
        tailwind.config.js
        package.json

    backend/
        main.py
        runner.py
        analyzer.py
        llm_agent.py
        plotting.py
        storage.py
        sample_scripts/
            train_example.py

    shared/
        schemas.py

    README.md
    requirements.txt
    .gitignore


=====================================================
FRONTEND — FULL IMPLEMENTATION
=====================================================

Use:
- Next.js (App Router)
- React
- Tailwind CSS
- TypeScript
- Plotly.js

The frontend is the main deliverable.

-----------------------------------------------------
UI LAYOUT
-----------------------------------------------------

The homepage (app/page.tsx) should be a **two-column grid**:

LEFT PANEL (40%)
- ChatWindow (entire chat interface)
- ChatInput at the bottom
- ChatBubble for messages

RIGHT PANEL (60%)
- RunsGrid (grid of run cards)
- Each RunCard shows:
  - run ID
  - hyperparameters (lr, epochs, batch size)
  - validation loss (placeholder)
  - learning rate (placeholder)
  - status (placeholder)
  - “View Plot” button linking to /run/[id]

Use Tailwind grid layout with a dark UI theme.

-----------------------------------------------------
COLOR PALETTE
-----------------------------------------------------
Apply consistently throughout the UI:

- #00143c  (dark navy background)
- #00b4f0  (bright blue accents)
- #b428ff  (purple highlights)

-----------------------------------------------------
PLOT VIEWER PAGE
-----------------------------------------------------

Create a dynamic route:
frontend/app/run/[id]/page.tsx

This page:
- fetches plot JSON from backend (placeholder endpoint)
- renders it using Plotly.js
- displays run metadata

-----------------------------------------------------
API SERVICE LAYER (FRONTEND ONLY)
-----------------------------------------------------

Create:
frontend/lib/api.ts

Include placeholder functions calling a backend that will exist later:

Example:

export async function getRuns() {
  return fetch("http://localhost:8000/runs").then(r => r.json());
}

export async function createRuns(configs) {
  return fetch("http://localhost:8000/run-job", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(configs)
  }).then(r => r.json());
}

These should NOT error — just be placeholder fetch calls.

-----------------------------------------------------
LLM ORCHESTRATOR (FRONTEND)
-----------------------------------------------------

Create:
frontend/lib/llm.ts

This file should include a stub function:

export async function proposeRunsFromUserMessage(message: string) {
  // TODO replace with OpenAI call
  // For now, return fake run configs
  return [
    { lr: 0.001, epochs: 5, batch_size: 32 },
    { lr: 0.0005, epochs: 5, batch_size: 32 }
  ];
}

-----------------------------------------------------
TYPES FILE
-----------------------------------------------------
Create:
frontend/lib/types.ts

Include basic TypeScript interfaces:

export interface RunConfig {
  lr: number;
  epochs: number;
  batch_size: number;
}

export interface Run {
  id: string;
  status: string;
  config: RunConfig;
  val_loss?: number;
  lr_used?: number;
}

=====================================================
BACKEND — ONLY PLACEHOLDER FILES
=====================================================

All backend Python files should be EMPTY except for docstrings/TODO comments.

Example backend/main.py:

"""
main.py — FastAPI entrypoint (placeholder)

Backend teammates will implement:

- POST /run-job
- GET /runs
- GET /run/{id}
- GET /plot/{id}
- WebSocket for logs

This file is intentionally left incomplete.
"""

Repeat similar structure for:
- runner.py
- analyzer.py
- llm_agent.py
- plotting.py
- storage.py

-----------------------------------------------------
INCLUDE SAMPLE TRAIN SCRIPT (REAL CODE HERE ONLY)
-----------------------------------------------------

backend/sample_scripts/train_example.py:

This file MUST contain a simple runnable script:

- argparse with 3-4 hyperparameters
- a fake training loop printing loss values
- no real ML, just placeholders

Example:

"""
usage:
python train_example.py --lr 0.001 --epochs 5 --batch_size 32
"""

Use prints like:
Epoch 1/5 - loss: 0.84
...

=====================================================
SHARED SCHEMAS
=====================================================

shared/schemas.py should contain an empty file with a placeholder comment.

=====================================================
README.md (GENERATE)
=====================================================

README must include:

- What Trex is
- Why it exists
- The ML researcher pain point
- How the chatbot helps produce multiple runs
- Explanation that backend is intentionally incomplete
- How to run the frontend
- How backend teammates should implement their modules
- API contract examples (REQUEST + RESPONSE shape)
- Color palette
- Architecture overview

=====================================================
OUTPUT REQUIREMENTS
=====================================================

Replit must produce:

- Full folder structure  
- Fully implemented frontend  
- Backend folder with only placeholder TODO files  
- Working Next.js project  
- Tailwind styling with Trex color palette  
- Plotly chart viewer page  
- Chat UI + Runs grid  
- README  
- GitHub-compatible project  
- Everything runnable inside Replit

=====================================================
END OF SPECIFICATION — generate all code now.
=====================================================
